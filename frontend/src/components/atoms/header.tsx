const navigation: { name: string; href: string }[] = [
  { name: "Swap", href: "#swap" },
  { name: "Exchange", href: "#exchange" },
  { name: "Transaction", href: "#transaction" },
];

export default function Header() {
  return (
    // <div className="flex justify-between bg-amber-100 gap-2 w-screen">
    //   <Button>Hello</Button>
    // </div>
    <div className="h-10 w-10 bg-red-50">sss</div>
  );
}
